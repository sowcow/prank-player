require 'cucumber/rake/task'

task :default => :run

desc 'run selenium before all'
task :pre do
  system 'sudo docker-compose up'
end

Cucumber::Rake::Task.new :run do |t|
  t.cucumber_opts = '--tags "not @wip and not @next"'
end

Cucumber::Rake::Task.new :wip do |t|
  t.cucumber_opts = '--tags "@wip"'
end

# Cucumber::Rake::Task.new :html do |t|
#   t.cucumber_opts = '--format html --tags "not @wip and not @next" --out report.html'
# end
